import { ILeaderBoard } from './ILeaderBoard';

export class LeaderBoard implements ILeaderBoard {
    columnToString(heading: string) {

        return '';
    }

    rowToString(rowNum: number) {
        return '';
    }

    tableToString() {
        return '';        
    }

    addRow() {

    }
}